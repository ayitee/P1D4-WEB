function capitalize(words) {
  return words.map(word => word.toUpperCase());
}
// This function takes an array of words, converts each word to uppercase,
// and returns a new array containing the capitalized words.
